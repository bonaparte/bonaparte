{
  "name": "bonaparte",
  
  "devDependencies": {
    "gulp": "*",
    "gulp-autoprefixer": "2.3.1"
  },

  "scripts": {
    "install" : "cd ui/ ; npm install; cd .. ; gulp build ;"
  },

  "engines": {
    "node": ">=0.12.x"
  }
}

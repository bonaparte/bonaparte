{
  "name": "bonaparte",
  
  "devDependencies": {
    "gulp": "*",
    "gulp-autoprefixer": "2.3.1",
    "gulp-less" : "^3.0.3"
  },

  "scripts": {
    "_install" : "cd ui/ ; npm install; cd .. ; gulp build ;"
  },

  "engines": {
    "node": ">=0.12.x"
  }
}

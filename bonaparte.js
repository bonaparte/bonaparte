// BASE BONAPARTE TAG

var bp = require("bonaparte");

///////////////////////////////////////////////////////////////////////////////
// Public

var mousetrap = require("mousetrap");

///////////////////////////////////////////////////////////////////////////////
// Public

// WHEN TAG IS PUT ON PAGE> COLLAPSABLE IS CALLED
module.exports = bp.tag.create("collapsible", collapsible);

///////////////////////////////////////////////////////////////////////////////
function collapsible(tag){

  // DOMREADY IS ONLY CALLED
  bp.tag.DOMReady(init);

///////////////////////////////////////////////////////////////////////////////

// WHEN ATTRIBUTE CHANGES - NEED TO REACT
  tag.addEventListener("bonaparte.tag.attributeChanged", attributeChangedCallback);  

///////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////

  function init(){
    // setEvents();
    // setToggles();
    // setTargets();
    // setAttributes();
    // setShortcut();
  }
}
